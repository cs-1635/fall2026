import 'dart:async';

// Starter 02: Periodic stream with .take(N)
/*
Prompt:
- Create a Stream.periodic that emits an integer index every 400ms.
- Use .take(5) so it completes.
- Consume it with await for and print 'tick <i>'.
- After it finishes, print 'complete'.
*/

Future<void> runExercise() async {
  // TODO: create periodic stream and .take(5)
  // final s = ...

  // TODO: await for to consume
  // await for (final t in s) {
  //   print(...);
  // }

  // TODO: final print
}

void main(List<String> args) async {
  runExample();
}